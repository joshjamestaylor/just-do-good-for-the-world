- [x] Add global colours that are editable in the CMS and can be generated as static site

  Done. Editable in Filament at **/admin/brand-settings** (Settings → Brand); served via
  `GET /api/v1/globals`; applied on the frontend as CSS variables that bake into the static
  build. See the "Global brand colours" section in README.md.
